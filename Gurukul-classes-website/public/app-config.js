window.__GURUKUL_RUNTIME_CONFIG__ = {
  // Set this to your backend URL on Hostinger, for example:
  // apiBaseUrl: "https://api.yourdomain.com"
  // Leave it empty only if /api is reverse-proxied to your backend on the same domain.
  apiBaseUrl: "",
};
