# Routes

This project uses a small client-side router in `src/App.tsx`.

Each file in this folder exports a plain React page component. Add new pages by
creating the component here and registering its path in the `ROUTES` map.
